--[=[
    请求验证 | 初始值
]=]


-- ip验证
if false then
    ngx.exit(403)
end

-- 请求头验证
if false then
    ngx.exit(403)
end

-- 解密验证
if false then
    ngx.say('{"code":"0","data":"","msg":"请求失败"}')
    ngx.exit(403)
end